public class Date {
    int day; // default = 1
    int month; // default = 1
    int year; // default = 0

    public Date(int day, int month, int year){
        this.day = day;
        this.month = month;
        this.year = year;
    }

    public int hashCode() {
        int hashCode;
        return hashCode;
    }


    @Override
    public boolean equals(Date date) {
        if (this = date) {
            return true;
        }
        if () {
            return false;
        }

    }

}
